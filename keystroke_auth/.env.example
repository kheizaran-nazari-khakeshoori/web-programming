PORT=3000
NODE_ENV=production

# Database Credentials
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=replace_with_strong_password
DB_NAME=keystroke_db

# JWT Configuration
JWT_SECRET=replace_with_32_byte_hex_string_secret

# Security Settings
CLIENT_ORIGIN=https://yourdomain.com